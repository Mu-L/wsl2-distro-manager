name: Publish to WinGet

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to publish (e.g. v1.11.0)
        required: true
        type: string
  release:
    types: [published, created]

concurrency:
  group: publish-winget-${{ github.event.release.tag_name || inputs.tag }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve tag and version
        id: resolve
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag }}"
          else
            TAG="${{ github.event.release.tag_name }}"
          fi

          if [[ -z "$TAG" ]]; then
            echo "Tag is empty" >&2
            exit 1
          fi

          VERSION="${TAG#v}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check WinGet token
        id: winget_token
        shell: bash
        env:
          WINGET_TOKEN: ${{ secrets.WINGET_TOKEN }}
        run: |
          if [ -z "$WINGET_TOKEN" ]; then
            echo "has_token=false" >> "$GITHUB_OUTPUT"
            echo "WINGET_TOKEN is not set. Skipping WinGet publish step."
          else
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          fi

      # The regex deliberately matches both setups a release carries —
      # wsl2-distro-manager-v<ver>-setup.exe and the -arm64-setup.exe beside it
      # — so the manifest gets an installer entry per architecture and an Arm64
      # PC stops being handed the emulated x64 build. The architecture is read
      # off the URL, which is why the Arm64 asset's name has to keep saying
      # "arm64". The Arm64 job attaches its asset before this workflow is
      # dispatched; if that job failed, the release simply has the one
      # installer and the manifest is what it always was.
      - name: Publish to WinGet
        if: steps.winget_token.outputs.has_token == 'true'
        uses: vedantmgoyal9/winget-releaser@v2
        with:
          identifier: Bostrot.WSLManager
          version: ${{ steps.resolve.outputs.version }}.0
          release-tag: ${{ steps.resolve.outputs.tag }}
          installers-regex: 'wsl2-distro-manager-v.*-setup\.exe$'
          token: ${{ secrets.WINGET_TOKEN }}
